
services:
  type: web
  plan: free
  name: InsightDocs_AI
  runtime: python
  buildCommand: './build.sh'
  startCommand: 'python -m gunicorn InsightDocs_AI.asgi:application -k uvicorn.workers.UvicornWorker'